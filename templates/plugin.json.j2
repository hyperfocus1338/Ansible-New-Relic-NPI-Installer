{
  "agents": [
    {
      "name"    : "Localhost",
      "host"    : "localhost",
      "metrics" : "status,newrelic",
      "user"    : "{{ newrelic_user }}",
      "passwd"  : "{{ newrelic_user_password }}"
    }
  ]
}
